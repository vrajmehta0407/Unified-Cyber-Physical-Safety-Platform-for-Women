import 'package:flutter/material.dart';

class AppColors {
  // Core brand
  static const primary = Color(0xFF7C3AED);
  static const primaryLight = Color(0xFF9D5CF6);
  static const primaryDark = Color(0xFF5B21B6);
  static const secondary = Color(0xFFEC4899);

  // Backgrounds
  static const background = Color(0xFF0F0A1A);
  static const surface = Color(0xFF1A1230);
  static const card = Color(0xFF221A3A);
  static const cardAlt = Color(0xFF1E1535);

  // Status
  static const danger = Color(0xFFEF4444);
  static const dangerDark = Color(0xFFDC2626);
  static const success = Color(0xFF22C55E);
  static const successDark = Color(0xFF16A34A);
  static const warning = Color(0xFFEAB308);
  static const info = Color(0xFF3B82F6);

  // Text
  static const textPrimary = Color(0xFFF8FAFC);
  static const textSecondary = Color(0xFF94A3B8);
  static const textHint = Color(0xFF64748B);

  // Borders
  static const border = Color(0xFF2D2450);
  static const borderLight = Color(0xFF3D3060);

  // Gradients
  static const List<Color> primaryGradient = [Color(0xFF7C3AED), Color(0xFFEC4899)];
  static const List<Color> dangerGradient = [Color(0xFFEF4444), Color(0xFFDC2626)];
  static const List<Color> darkGradient = [Color(0xFF0F0A1A), Color(0xFF1A1230)];
  static const List<Color> cardGradient = [Color(0xFF221A3A), Color(0xFF1A1230)];
  static const List<Color> successGradient = [Color(0xFF22C55E), Color(0xFF16A34A)];

  // Glassmorphism overlay
  static const glassOverlay = Color(0x1AFFFFFF);
  static const glassBorder = Color(0x33FFFFFF);

  // Risk levels
  static const riskHigh = Color(0xFFEF4444);
  static const riskMedium = Color(0xFFEAB308);
  static const riskLow = Color(0xFF22C55E);
}
